---
all:
  hosts:
    localhost:
      ansible_connection: local
#    This is an example entry
#    raspi:
#      ansible_host: 192.168.0.10
#      ansible_user: pi

  children:
    desktops:
      hosts:
        localhost:
